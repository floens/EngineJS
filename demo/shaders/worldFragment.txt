precision highp float;

varying vec3 vColor;
varying vec2 vTexture;

uniform sampler2D uSampler;

void main() {
    gl_FragColor = texture2D(uSampler, vec2(vTexture.x / 16.0, vTexture.y / 16.0)) * vec4(vColor, 1.0);
}
